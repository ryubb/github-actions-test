name: test
on:
  pull_request:
    types: [labeled]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: ryubb/github-actions-test@v0.1.3
      with:
        access-token: ${{ secrets.GITHUB_TOKEN }}
