name: test
on:
  pull_request:
    types: [labeled]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: ryubb/github-actions-test@v0.1.0
      with:
        access-token: ${{ secrets.ACCESS_TOKEN }}
